{
  "name": "voxtell-seg",
  "displayName": "VoxTell & nnInteractive Segmentation",
  "version": "0.2.0",
  "description": "Run VoxTell text-prompted and nnInteractive bbox-prompted 3-D medical image segmentation as callable tools, with request validation that refuses anatomically impossible prompts before any compute is spent.",
  "author": {
    "name": "Brian Xiao"
  },
  "repository": "https://github.com/pentaexe/VoxTell-main",
  "license": "Apache-2.0",
  "keywords": [
    "medical-imaging",
    "segmentation",
    "voxtell",
    "nninteractive",
    "mcp"
  ],
  "userConfig": {
    "voxtell_python": {
      "type": "string",
      "title": "Python interpreter (set this to a full path)",
      "description": "Full path to the interpreter where you ran 'pip install -e .' from the VoxTell checkout. No bare name works everywhere: on Ubuntu 'python' usually does not exist, and on Windows python/python3/py commonly resolve to the Microsoft Store stub, which exits without running. If no tools appear after install, this is why. Run skills/voxtell-inference/scripts/doctor.py to get the exact value.",
      "default": "python3"
    },
    "voxtell_model_dir": {
      "type": "string",
      "title": "VoxTell checkpoint directory (optional)",
      "description": "Folder containing plans.json and fold_0/checkpoint_final.pth. Leave blank and the setup tool will download it from Hugging Face to ~/.cache/voxtell_models.",
      "default": ""
    },
    "nninteractive_weights": {
      "type": "string",
      "title": "nnInteractive weights directory (optional)",
      "description": "Folder containing the nnInteractive_v1.0 checkpoint, which must include fold_all. Leave blank to disable nninteractive_segment.",
      "default": ""
    }
  }
}
